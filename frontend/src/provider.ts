export enum RouteNameProvider{
    home = 'home',
    products = 'products',
    customers = 'customers'
}

export enum RoutePathProvider{
    home = '/',
    products = '/products',
    customers = '/customers'
}

