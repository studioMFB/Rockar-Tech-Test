export enum PassportUrl {
    root = 'https://passport.visr.services',
    login = 'https://passport.visr.services/login',
}

export enum CoreUrl {
    root = 'https://core.visr.services',
}

export enum DashboardRoute {
    base = '/',
    userMe = '/user/me',
    customiseAvatar = '/user/me/customiseavatar',
    project = '/project',
    projects = '/user/me/projects',
    createProject = '/user/me/createproject',
}

export enum Button {
    createNewAccount = 'Create a new Account',
    signInWithMicrosoftAccount = 'Sign in with Microsoft Account',
    signInToPassport = 'Sign in to Passport',
    continueWith = 'Continue with...',
    signOut = 'Sign Out',
    random = 'Random',
    done = 'Done',
    cancel = 'Cancel',
    x = '✖',
    create = 'Create',
    next = 'Next',
    newProject = 'New Project',
    newObject = 'New Object',
    deleteProject = 'Delete Project',
}
